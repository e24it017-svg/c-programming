#include <stdio.h>
int main() {
    int i,n,sum=0;
    printf("input:");
    scanf("%d",&n);
    for(i=n;i!=0;i=i/10){
      sum=sum+(i%10);
    }
    printf("output:%d",sum);   
return 0;

}