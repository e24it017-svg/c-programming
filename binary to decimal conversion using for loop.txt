#include <stdio.h>
#include <math.h>
int main() {
    int n,r,d=0,i;
    printf("input:");
    scanf("%d",&n);
    for(i=0;n!=0;i++){
       r=n%10;
       d=d+r*pow(2,i);
       n=n/10;
    }
    printf("output:%d",d);
    return 0;
}