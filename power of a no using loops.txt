#include <stdio.h>
int main() {
    int pow=1,a;
    int n,i;
    printf("input:");
    scanf("%d%d",&n,&a);
    for(i=1;i<=a;i++)
      pow=pow*n;
    printf("output:%d\n",pow);

    return 0;
}