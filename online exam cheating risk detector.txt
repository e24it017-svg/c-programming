#include<stdio.h>
int main(){
    int i, n,a,count=0,total=0;
    printf("input:");
    scanf("%d",&n);
    for(i=0;i<n;i++){
    scanf("%d",&a);
    total+=a;
    if(a>50){
      count++;
    }
    }
    printf("total risk:%d\n",total);
    printf("high risk session:%d",count);
    return 0;
}