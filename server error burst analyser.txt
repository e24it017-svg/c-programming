#include<stdio.h>
int main(){
    int i,n,a,total=0,count=0;
    printf("input:");
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&a);
        total=total+a;
        if(a>50){
        count++;
        }
   }
   printf("total errors:%d\n",total); 
   printf("crittical hours:%d",count);
    return 0;
}
