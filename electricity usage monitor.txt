#include <stdio.h>
int main() {
    int n,a,total=0;
    printf("input:");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&a);
        total=total+a;
    }
    printf("Total Units:%d",total);

    return 0;
}