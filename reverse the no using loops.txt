#include <stdio.h>
int main() {
    int i,n,rev=0;
    printf("input:");
    scanf("%d",&n);
    for(i=n;i!=0;i=i/10){
        int rem=i%10;
    rev=rev*10+rem;
    }
    printf("output:%d",rev);   
return 0;
}